# 🔐 GitHub 仓库隐私安全审计报告

**审计时间**: 2026-05-18 18:00
**审计对象**: YD4223 全部 6 个仓库

---

## 审计结果总览

| 仓库 | 风险等级 | 结论 |
|------|---------|------|
| **aihub** | ✅ 安全 | 凭据均在 `.env` 且被 `.gitignore` 保护 |
| **walkie-talkie** | ⚠️ **需注意** | 前端硬编码了 Supabase URL 和匿名密钥 |
| **ATgames** | ✅ 安全 | 纯静态站点，无敏感信息 |
| **tzg-tv** | ✅ 安全 | 只有公开的电视直播流 URL |
| **tongzhenge** | ✅ 安全 | 空仓库 |
| **pstam-cf-tunnel-sub** | ✅ 安全 | Fork 项目，只使用了环境变量 |

---

## 逐仓库详细说明

### 1. aihub ✅ 安全
- `.env` / `.env.bak` 均被 `.gitignore` 保护，从未进入 Git 历史
- 所有真实凭据通过 `process.env.xxx` 引用，代码中无硬编码
- Google/Bing 搜索不到任何泄露的凭据

**建议**: 
- 删除本地的 `.env.bak`（保留着旧的 Supabase 密码）
- GitHub token `ghp_***`（已隐藏）如果不用了就去 GitHub Settings 删掉

### 2. walkie-talkie ⚠️ 需注意
- **问题**: `index.html` 中硬编码了 Supabase 连接信息
  - Supabase URL: `https://hogtlfhzaptjmdofirpc.supabase.co`
  - Supabase 匿名密钥: `eyJhbGciOiJ...`
- 匿名密钥有 RLS (Row Level Security) 限制，不是完全公开的凭据，但仍属于敏感信息
- 登录页正常运作，聊天页面可访问

**建议**: 
- 将 Supabase URL 和匿名密钥移到环境变量，不要在 HTML 里写死
- 检查 Supabase 的 RLS 策略是否严格限制了匿名用户权限

### 3. ATgames ✅ 安全
- 纯静态 HTML 资源站，无 API 调用
- `README.md` 中引用了外部 RapidAPI 链接，无密钥泄露
- 代码中只有 `508002830`（你的公开 QQ 号）和百度网盘提取码 `SMDZ`（已公开的）

### 4. tzg-tv ✅ 安全
- 电视直播导航站，只有公开的流媒体 URL
- 完全无后端交互，无凭据

### 5. tongzhenge ✅ 安全
- 空仓库，什么都没放

### 6. pstam-cf-tunnel-sub ✅ 安全
- Fork 自上游，未修改敏感配置
- `wrangler.toml` 只有占位符
- `vercel.json` 只有路由规则
- `proxyip.txt` 存的是公开代理 IP 列表

---

## 整体结论

**你的个人信息没有被泄露到公开网络上。** GitHub 个人资料也很干净（没填真实姓名、邮箱、位置）。主要风险点在于 **walkie-talkie 的 Supabase 匿名密钥**，虽然它设计上就是给客户端用的密钥，硬编码不推荐但不是致命问题。

### ⚡ 优先建议

1. **删掉本地的 `.env.bak`**
2. **去 GitHub Settings 把 `ghp_***` token 删掉**（如果不用了）
3. **walkie-talkie 的 Supabase 配置** — 如果你还在维护这个项目，建议改环境变量注入

其他都没问题。
